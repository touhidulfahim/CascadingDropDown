@model IEnumerable<CascadingDropDownApp.Models.CategoryModels>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Categories.ItemCategoryName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ItemCategoryName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ShortName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Categories.ItemCategoryName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ItemCategoryName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ShortName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ItemCategoryID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ItemCategoryID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ItemCategoryID })
        </td>
    </tr>
}

</table>
